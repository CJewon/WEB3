변수 : 변하는 수
ex> 나이는 매년 바뀐다. 그러므로 변수이다.

상수 : 변하면 안되는 수
ex> 생년월일은 변할 수 없다. 그러므로 상수이다.


var age = 30 : age라는 이름으로 변수를 하나 만들겠다.

age = age + 1 -> age = 31

let : 변수를 선언
const : 상수를 선언
-> 
const 출생년도 = 1993;
let 나이;

나이 = 현재년도 - 출생년도 + 1;
